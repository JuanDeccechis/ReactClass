import { types } from "./actions";

const initialState = {
  users: [],
  loadingUsers: false,
  selectedUser: {},
  error: '',
  posts: [],
  loadingPosts: false
};

const reducer = (state = initialState, action) => {
  

  return state;
};

export default reducer;
